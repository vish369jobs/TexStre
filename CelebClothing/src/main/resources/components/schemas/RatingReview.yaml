RatingReview:
  type: object
  properties:
    ratingReviewId:
      type: integer
      format: int64
    productId:
      type: integer
      format: int64
    rating:
      type: number
      format: float
    review:
      type: string
    createdAt:
      type: string
    createdById:
      type: integer
      format: int64
