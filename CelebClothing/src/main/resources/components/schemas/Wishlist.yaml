Wishlist:
  type: object
  properties:
    wishlistId:
      type: integer
      format: int64
    userId:
      type: integer
      format: int64
    wishlistName:
      type: string

WishlistItem:
  type: object
  properties:
    wishlistItemId:
      type: integer
      format: int64
    wishlistId:
      type: integer
      format: int64
    productId:
      type: integer
      format: int64
    isWishlisted:
      type: boolean